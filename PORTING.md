# GangLand → Web: сводка о портировании и рекомендуемый тул-чейн

> Дата: 2026-08-31. Основано на фактическом анализе GOG-версии GangLand (MediaMob Studios, 2004),
> лежащей в корне `C:\Meine\gangland-online`.

---

## 0. Статус выполнения (обновлено 2026-08-31, фаза 1 закрыта динамическим методом)

✅ **Фазы 0–1 выполнены** (см. [README.md](README.md)).

- Формат `.lfm` восстановлен полностью; 7 открытых архивов извлечены (~288 MB).
- DDS-декодер DXT1/3/5 в браузере: все 2962 текстуры просматриваются без конвертации.
- **Каталоги всех 5 «шифрованных» архивов восстановлены из памяти запущенного процесса**
  (`tools/recover-from-dump.mjs`; суммы размеров сходятся байт-в-байт): 1234 файла игры
  поименованы и отмерены.
- **94/107 игровых Lua-скриптов расшифрованы** (байткод Lua 5.0 с кастомной шапкой) —
  ядро игровой логики. **maps.lfm разобран на 559 файлов**, включая 37 карт кампании
  с настоящими именами (Vegas/Little Italy/Poor/Docks/Ninja).
- Осталось шифрованным: тексты (.ini), характеристики юнитов, локаль (.lcl), map-скрипты.
  Шифр не XOR/не MSVC-LCG; пути добора — дампы после загрузки контента (быстро) или
  реверс шифра (теперь ~180 КБ известных пар «шифр↔текст»).
- Урок фазы 1: статический реверс (Ghidra headless развёрнут в `C:\Meine\tools`, скрипты
  `FindLfmLoader/DecompAt/FindXrefs`) дал карту кода, но дешифратор найти по строкам не удалось
  (строки без xref-ов, string pooling). Динамический дамп памяти решил задачу за минуты.

---

## 1. Технический профиль игры (что внутри)

| Компонент | Факт | Следствие для порта |
|---|---|---|
| Движок | Кастомный C++, 32-bit i386, exe **не запакован** | Дизассемблируется (Ghidra) без распаковки |
| Рендер | `RendererD3D.DLL` (Direct3D 8) / `RendererOGL.DLL` (классический OpenGL, immediate mode) | Съёмный рендерер — логика не привязана к графике |
| Скрипты | **Lua 5.0** вшита в exe; игровые скрипты — обычный текст (примеры в `Custom/Example/*.lua`) | Игровая логика частично переносится почти как есть |
| Звук | FMOD 3.x, `.wav` в архивах + `.ogg` в `Data/streams` | Форматы проигрываются в браузере напрямую (Web Audio API) |
| Сеть | GameSpy + DirectPlay (мёртвые сервисы) | Онлайн-часть игры всё равно нужно переписывать — WebSockets |
| Ресурсы | Архивы `.lfm`: `uint32 + [uint24 size + имя + данные]*` | Большинство читаются: PNG, TGA, WAV, шрифты `.mmf` |
| Зашифрованные архивы | `lua.lfm`, `text.lfm`, `characters.lfm`, `maps.lfm`, `locale.lfm` (энтропия 7.71 — сжатие+обфускация) | Вскрываются реверсом загрузчика в Ghidra; exe не запакован — выполнимо |
| Карты | Формат `FWMP` (`.fmp`) — открытый, с текстовыми путями; `.rmp` — сырые тайлы; `.scm`/`.vis` — генерируемые | Карты парсятся напрямую, редакторский пайплайн описан в `Map Editor Manual.pdf` |

**Ключевой вывод:** большая часть контента (текстуры, звук, UI, модели, карты) извлекается скриптом за день.
Игровые правила заданы на Lua — языке, который выполняется и в браузере. Это редкий удачный случай для порта.

---

## 2. Вердикт: портировать можно, вопрос — в какой степени

### Вариант A — «Web-ремейк» (рекомендуется) ✅
Ассеты извлекаются из `.lfm`, геймплей переписывается на TypeScript по поведению оригинала
(правила боёв, экономика, иерархия банд, коп, продажа бизнеса — всё понятно из Lua-скриптов и геймплея).
Мультиплеер — WebSockets. Оценка: реалистично силами 1–2 разработчиков за 3–6 месяцев до играбельной демки
одного режима (Skirmish/Shootout), т.к.:
- тайловая RTS 2004 года — относительно простая симуляция по меркам сегодня;
- структура карт, юнитов и настроек видна в открытых ini/lua;
- не требуется пиксель-в-пиксель совместимость с оригиналом.

### Вариант B — Гибрид: оригинальные Lua-скрипты в браузере 🔶
Нативная логика (боёвка, ИИ) всё равно в C++ внутри exe, поэтому полностью «родную» логику не получить,
но слой `mapsetup`/режимов можно выполнять в настоящей Lua VM (WASM) с мостом к нашему движку.
Нюанс: скрипты написаны под **Lua 5.0** (`for i,v in t do`, `table.getn`, `math.mod`) — эти идиомы удалены
в Lua 5.1+. Варианты: WASM-сборка Lua 5.0, либо механическая адаптация скриптов под 5.4 (Wasmoon).

### Вариант C — Эмуляция (Boxedwine / wine в WASM) ❌
Технически возможно (GOG-версия старая), но: сотни МБ, тормоза, мобильные устройства отпадают,
GameSpy-онлайн мёртв, юридически сомнительно размещать exe. Не рекомендуется — годится только как
временный «поиграть в оригинал» режим для себя.

---

## 3. Рекомендуемый тул-чейн

### Клиент
| Слой | Выбор | Почему |
|---|---|---|
| Язык/сборка | **TypeScript + Vite** | Стандарт, быстрый HMR, бесплатный |
| Рендер 2D (изометрия/тайлы, спрайты) | **PixiJS** | Идеален для тайловой RTS, легко 1000+ спрайтов |
| Рендер 3D (если тянуть родные модели из `models.lfm`) | **Three.js**, модели → glTF | Оригинал — 3D-модели на тайлах; конвертер `.mmd`/моделей напишем сами |
| Lua-слой (вариант B) | **Wasmoon** (Lua 5.4, WASM) или wasm-сборка Lua 5.0 | Выполнять оригинальные скрипты режимов |
| UI игры | React + Tailwind (или чистый PixiJS UI) | Меню/лобби/магазин быстрее на React |
| Звук | Web Audio API (нативные `.wav`/`.ogg`) | Конвертация не нужна |
| Тайлы/карты | Свой парсер `FWMP` + редакторская документация | Формат открыт, пример `Custom/Example/01.fmp` есть |

### Сервер / мультиплеер
| Задача | Выбор | Почему |
|---|---|---|
| Комнаты реального времени | **Cloudflare Durable Objects** (есть на Free-плане) | Настоящие WebSocket-серверы с состоянием, идеальны для RTS-матчей |
| Авторизация, матч-мейкинг REST | Cloudflare Workers / Vercel Functions | Бесплатные лимиты с запасом для инди |
| Постоянные данные (профили, рейтинг, сохранения) | **Neon Postgres** (Free) | Серверless Postgres, ветвление БД для дев/прод |
| Раздача ассетов (после конвертации) | **Cloudflare R2** (10 GB free, без egress-платежей) | Дешевле всего хранить текстуры/звук |
| Статика клиента | **Cloudflare Pages** или **Vercel** | Оба бесплатны, CI из GitHub |
| Анти-чит/валидация ходов | Сервер-авторитарная симуляция в DO | RTS требует серверной проверки ходов |
| Чат | Тот же DO (канал лобби) | Не нужен отдельный сервис |

### Итоговая рекомендация
**Cloudflare-стек как основа** (Pages + Workers + Durable Objects + D1/R2) + **Neon Postgres** для данных —
всё бесплатно на этапе разработки и готово к realtime. Vercel — равнозначная альтернатива фронтенда,
но его serverless-модель хуже подходит для долгоживущих WebSocket-комнат, поэтому realtime на CF.

Схема: клиент (PixiJS/TS) ⇄ WSS ↔ Durable Object «GameRoom» (тик 10–20 Гц, snapshot/дельты) ⇄ D1/Neon (результаты, профили). Ассеты — R2 + длинный кэш.

---

## 4. Бесплатные инструменты и сервисы для порта

| Сервис | Бесплатный лимит | Роль в проекте |
|---|---|---|
| Cloudflare Pages/Workers | Безлимит статики; 100k req/день | Хостинг клиента, API |
| Cloudflare Durable Objects | Есть на Free-плане | Realtime-комнаты матчей |
| Cloudflare R2 | 10 GB, egress бесплатный | Хранилище конвертированных ассетов |
| Cloudflare D1 | 5 GB SQLite | Лёгкие данные (лобби, метч-хистори) |
| Neon Postgres | ~0.5 GB, автоскейл в 0 | Профили, рейтинги, сохранения |
| Vercel (Hobby) | Безлимит статики, serverless | Альтернатива CF для клиента/превью на PR |
| Supabase (Free) | 500 MB Postgres + Auth realtime | Альтернатива «Neon + Auth + Realtime» одним пакетом |
| GitHub (Actions, Pages) | Бесплатно для публичных | CI/CD, хостинг демо |
| Ghidra | Полностью бесплатна (NSA) | Реверс загрузчика `.lfm` (расшифровка lua/text/characters/maps) |
| x64dbg, Cheat Engine | Бесплатно | Рантайм-анализ, дамп расшифрованных данных из памяти |
| TexturePacker / Sharp / ffmpeg | Бесплатно | Конвертация PNG/TGA → webp/atlas, WAV → ogg/opus |
| Wasmoon, Fengari, lua.wasm | Бесплатно, MIT | Lua VM в браузере |
| Blender | Бесплатно | Правка/конвертация извлечённых моделей в glTF |

---

## 5. Пайплайн извлечения ассетов (первые шаги)

1. **Парсер `.lfm`** (Node/TS): заголовок `uint32`, далее записи `uint24 размер + NUL-имя + данные`.
   Открытые архивы: `game_textures`, `user_interface`, `models`, `sound`, `animation`, `effect_textures`, `fonts` — уже содержат PNG/TGA/WAV.
2. **Расшифровка** `lua.lfm`, `text.lfm`, `characters.lfm`, `maps.lfm`, `locale.lfm`:
   открыть `GangLand.exe` в Ghidra, найти загрузчик по строкам `data/lua/...`/`ReadFile`, восстановить алгоритм
   (энтропия 7.71 указывает на сжатие после обфускации; в exe есть zlib 1.1.4 — распаковщик уже внутри).
   Fallback: дамп уже расшифрованных данных из памяти процесса через x64dbg/Cheat Engine.
3. **Конвертация**: TGA/PNG → webp-атласы; WAV → opus/ogg; шрифты `.mmf` → canvas-атласы/веб-шрифты;
   карты `.fmp` → JSON; Lua-скрипты → выполнение в Lua VM или ручной перенос в TS.

---

## 6. Дорожная карта (оценка)

| Этап | Содержание | Оценка |
|---|---|---|
| 0 | Парсер .lfm, извлечение открытых ассетов, просмотр в браузере (галерея текстур/звука) | 1–2 недели |
| 1 | Реверс расшифровки .lfm в Ghidra → полный доступ к lua/characters/text/maps | 1–3 недели |
| 2 | Парсер FWMP-карт + рендер тайлов/зданий в PixiJS | 2–4 недели |
| 3 | Ядро симуляции на TS: юниты, приказы, боёвка, здания, деньги | 6–10 недель |
| 4 | UI: выбор юнитов, приказы, магазин, миникарта, лобби | 4–6 недель |
| 5 | Мультиплеер: DO-комнаты, синхронизация, rematch | 3–5 недель |
| 6 | Полировка, баланс, деплой на CF Pages + Neon | постоянно |

**Итого: играбельная web-демка Skirmish-режима — ~3–4 месяца одного разработчика; полный паритет — дольше, но без блокеров.**

---

## 7. Правовой момент

- Права на игру и ассеты принадлежат правообладателю (разработчик MediaMob Studios, издатель эпохи — Got Game Entertainment; текущие права после GOG-дистрибуции лучше проверить).
- Личный, некоммерческий порт «для себя» — серая, но общепринятая зона моддинга; **публиковать извлечённые ассеты и exe нельзя**.
- Безопасная модель публикации: код движка/сервера — свой (MIT), ассеты у игрока не нужны, если сделать собственные заглушки; либо публичная демка только на переписанных ассетах.

---

## 8. Открытые вопросы / риски

1. **Расшифровка 5 архивов** — единственная technically-blocker-задача реверса; при неудаче кампания/тексты восстанавливаются вручную по геймплею (медленно). Оценка успеха высокая: exe не запакован.
2. **Lua 5.0 идиомы** — скрипты не запустятся на современных VM без адаптации.
3. **ИИ противников** — полностью в нативном коде; переносится только переписыванием по поведению (самая трудоёмкая часть).
4. **3D-модели** — формат моделей в `models.lfm` не разобран; для спрайтового ремейка не нужен, для 3D-ремейка нужен конвертер.
